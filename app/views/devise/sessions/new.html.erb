<div class="box mt-5 m-4 w-50 mx-auto h-100 rounded-5 p-5">
<div class=" mx-auto w-75">
<h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { placeholder: "Email", autocomplete: "email", class: "pill-input" } %>

    <%= f.input :password,
                required: false,
                input_html: { placeholder: "current-password", autocomplete: "current-password", class: "pill-input" } %>

    <% if devise_mapping.rememberable? %>
      <div class="form-check mt-2 remember-me">
        <%= f.check_box :remember_me, class: "form-check-input", id: "rememberMe" %>
        <%= f.label :remember_me, "Remember me", class: "form-check-label", for: "rememberMe" %>
      </div>
    <% end %>
  </div>



  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "btn btn-dark rounded-5 mt-3 w-100 mb-3" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
